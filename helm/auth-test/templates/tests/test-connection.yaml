apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "auth-test.fullname" . }}-test-connection"
  labels:
    {{- include "auth-test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "auth-test.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
