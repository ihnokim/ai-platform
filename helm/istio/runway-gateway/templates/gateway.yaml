apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: {{ include "runway-gateway.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "runway-gateway.labels" . | nindent 4 }}
spec:
  selector:
    istio: gateway
  servers:
    {{- include "runway-gateway.servers" . | indent 4 }}
