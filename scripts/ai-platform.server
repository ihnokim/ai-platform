${DOMAIN_HOST} {
    template IN A {
        match "^.*\.${DOMAIN_HOST_ESCAPED}\.$"
        answer "{{ .Name }} 60 IN A ${GATEWAY_IP}"
        fallthrough
    }
    template IN A {
        match "^${DOMAIN_HOST_ESCAPED}\.$"
        answer "{{ .Name }} 60 IN A ${GATEWAY_IP}"
        fallthrough
    }
}
